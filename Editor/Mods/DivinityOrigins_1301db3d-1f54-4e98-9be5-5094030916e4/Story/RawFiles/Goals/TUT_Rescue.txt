Version 1
SubGoalCombiner SGC_AND
INITSECTION

KBSECTION
IF
TextEventSet("TUT_OriginEscape")
THEN
TimerLaunch("TUT_BreakCrate",0);


// Rescue becomes possible when someone chooses to save the others
// (avoid activating subgoals if set via debug book in Fort Joy)

IF
TimerFinished("TUT_BreakCrate")
AND
DB_CurrentLevel("TUT_Tutorial_A")
THEN
GoalCompleted;

// No GoalCompleted on RegionEnded, because otherwise the subgoals become active!
EXITSECTION

ENDEXITSECTION
ParentTargetEdge "Tutorial_PrisonShip"
